---
phase: 08-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Govorun/Models/SuperTextStyle.swift
  - Govorun/Views/SettingsTheme.swift
  - GovorunTests/SuperTextStyleTests.swift
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "SuperTextStyle.cardDescription returns Russian descriptions for all 3 styles"
    - "SuperStyleMode.displayName returns 'Авто' and 'Ручной'"
    - "SettingsSection.textStyle exists with correct title, subtitle, icon"
    - "SettingsSection.visibleCases includes textStyle after general"
  artifacts:
    - path: "Govorun/Models/SuperTextStyle.swift"
      provides: "cardDescription and displayName computed properties"
      contains: "var cardDescription: String"
    - path: "Govorun/Views/SettingsTheme.swift"
      provides: "textStyle case in SettingsSection enum"
      contains: "case textStyle"
    - path: "GovorunTests/SuperTextStyleTests.swift"
      provides: "Tests for cardDescription and displayName"
      contains: "test_card_description"
  key_links:
    - from: "Govorun/Views/SettingsTheme.swift"
      to: "Govorun/Models/SuperTextStyle.swift"
      via: "SettingsSection.textStyle uses SuperTextStyle display properties"
      pattern: "case textStyle"
---

<objective>
Add model properties (cardDescription on SuperTextStyle, displayName on SuperStyleMode) and SettingsSection.textStyle enum case needed by the UI view in Plan 02.

Purpose: Plan 02 (the SwiftUI view) depends on these model properties and enum case existing. Splitting model+enum from view keeps each plan focused.
Output: Extended SuperTextStyle.swift, extended SettingsTheme.swift, extended SuperTextStyleTests.swift
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui/08-CONTEXT.md
@.planning/phases/08-ui/08-UI-SPEC.md

<interfaces>
<!-- From Govorun/Models/SuperTextStyle.swift -->
enum SuperTextStyle: String, CaseIterable, Codable {
    case relaxed
    case normal
    case formal
    var displayName: String  // already exists: "Расслабленный", "Обычный", "Формальный"
}

enum SuperStyleMode: String, CaseIterable {
    case auto
    case manual
}

<!-- From Govorun/Views/SettingsTheme.swift -->
enum SettingsSection: String, Identifiable {
    case general
    case appModes
    case dictionary
    case snippets
    case history
    static let visibleCases: [SettingsSection] = [.general, .dictionary, .snippets, .history]
    var title: String
    var subtitle: String
    var icon: String
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cardDescription to SuperTextStyle and displayName to SuperStyleMode with TDD tests</name>
  <files>Govorun/Models/SuperTextStyle.swift, GovorunTests/SuperTextStyleTests.swift</files>
  <read_first>
    - Govorun/Models/SuperTextStyle.swift (current model — see existing extensions)
    - GovorunTests/SuperTextStyleTests.swift (existing tests — add new tests at end)
    - .planning/phases/08-ui/08-UI-SPEC.md (exact copy values from Copywriting Contract)
  </read_first>
  <behavior>
    - test_card_description_relaxed: SuperTextStyle.relaxed.cardDescription == "Как в мессенджере — строчные буквы, бренды кириллицей, без точки"
    - test_card_description_normal: SuperTextStyle.normal.cardDescription == "Стандартный — заглавная буква, бренды оригинальные, без точки"
    - test_card_description_formal: SuperTextStyle.formal.cardDescription == "Деловой — заглавная буква, бренды оригинальные, сленг раскрыт, точка в конце"
    - test_super_style_mode_display_name_auto: SuperStyleMode.auto.displayName == "Авто"
    - test_super_style_mode_display_name_manual: SuperStyleMode.manual.displayName == "Ручной"
  </behavior>
  <action>
1. In GovorunTests/SuperTextStyleTests.swift, add a new MARK section `// MARK: - cardDescription` after the existing styleBlock tests. Add three tests:
   - `test_card_description_relaxed` asserting exact string "Как в мессенджере — строчные буквы, бренды кириллицей, без точки"
   - `test_card_description_normal` asserting exact string "Стандартный — заглавная буква, бренды оригинальные, без точки"
   - `test_card_description_formal` asserting exact string "Деловой — заглавная буква, бренды оригинальные, сленг раскрыт, точка в конце"

2. Add another MARK section `// MARK: - SuperStyleMode displayName` with two tests:
   - `test_super_style_mode_display_name_auto` asserting `SuperStyleMode.auto.displayName == "Авто"`
   - `test_super_style_mode_display_name_manual` asserting `SuperStyleMode.manual.displayName == "Ручной"`

3. Run tests — they MUST FAIL (RED).

4. In Govorun/Models/SuperTextStyle.swift, add computed property `cardDescription: String` inside the existing `extension SuperTextStyle` block (the one with `contract`, `displayName`, `terminalPeriod`):
   ```swift
   var cardDescription: String {
       switch self {
       case .relaxed: "Как в мессенджере — строчные буквы, бренды кириллицей, без точки"
       case .normal: "Стандартный — заглавная буква, бренды оригинальные, без точки"
       case .formal: "Деловой — заглавная буква, бренды оригинальные, сленг раскрыт, точка в конце"
       }
   }
   ```

5. Add `displayName` as a computed property on `SuperStyleMode` via a new extension after the enum declaration:
   ```swift
   extension SuperStyleMode {
       var displayName: String {
           switch self {
           case .auto: "Авто"
           case .manual: "Ручной"
           }
       }
   }
   ```

6. Run tests — they MUST PASS (GREEN).
  </action>
  <acceptance_criteria>
    - Govorun/Models/SuperTextStyle.swift contains `var cardDescription: String`
    - Govorun/Models/SuperTextStyle.swift contains the exact string "Как в мессенджере — строчные буквы, бренды кириллицей, без точки"
    - Govorun/Models/SuperTextStyle.swift contains the exact string "Стандартный — заглавная буква, бренды оригинальные, без точки"
    - Govorun/Models/SuperTextStyle.swift contains the exact string "Деловой — заглавная буква, бренды оригинальные, сленг раскрыт, точка в конце"
    - Govorun/Models/SuperTextStyle.swift contains `extension SuperStyleMode` with `var displayName: String`
    - Govorun/Models/SuperTextStyle.swift contains the exact string `case .auto: "Авто"`
    - Govorun/Models/SuperTextStyle.swift contains the exact string `case .manual: "Ручной"`
    - GovorunTests/SuperTextStyleTests.swift contains `func test_card_description_relaxed()`
    - GovorunTests/SuperTextStyleTests.swift contains `func test_super_style_mode_display_name_auto()`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation -only-testing:GovorunTests/SuperTextStyleTests 2>&1 | tail -5</automated>
  </verify>
  <done>SuperTextStyle has cardDescription for all 3 styles, SuperStyleMode has displayName for both cases, 5 new tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add textStyle case to SettingsSection enum</name>
  <files>Govorun/Views/SettingsTheme.swift</files>
  <read_first>
    - Govorun/Views/SettingsTheme.swift (current enum — see visibleCases, title, subtitle, icon switches)
    - .planning/phases/08-ui/08-UI-SPEC.md (Copywriting Contract for exact sidebar title/subtitle/icon values)
  </read_first>
  <action>
1. In Govorun/Views/SettingsTheme.swift, add `case textStyle` to the `SettingsSection` enum, after `case history`.

2. Update `visibleCases` array — insert `.textStyle` at position index 1 (after `.general`, before `.dictionary`):
   ```swift
   static let visibleCases: [SettingsSection] = [.general, .textStyle, .dictionary, .snippets, .history]
   ```

3. Add cases to all three switch statements:
   - `title`: `case .textStyle: "Стиль текста"`
   - `subtitle`: `case .textStyle: "Настройка стиля для Супер-режима"`
   - `icon`: `case .textStyle: "textformat"`

4. Do NOT modify SettingsView.swift yet — Plan 02 handles that with the actual view.
  </action>
  <acceptance_criteria>
    - Govorun/Views/SettingsTheme.swift contains `case textStyle`
    - Govorun/Views/SettingsTheme.swift contains `.textStyle` in visibleCases array
    - Govorun/Views/SettingsTheme.swift contains `case .textStyle: "Стиль текста"`
    - Govorun/Views/SettingsTheme.swift contains `case .textStyle: "Настройка стиля для Супер-режима"`
    - Govorun/Views/SettingsTheme.swift contains `case .textStyle: "textformat"`
    - visibleCases order is [.general, .textStyle, .dictionary, .snippets, .history]
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation -only-testing:GovorunTests/SuperTextStyleTests 2>&1 | tail -5</automated>
  </verify>
  <done>SettingsSection.textStyle exists with title "Стиль текста", subtitle "Настройка стиля для Супер-режима", icon "textformat", visible in sidebar at position after general</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation` — full suite green
- grep "case textStyle" Govorun/Views/SettingsTheme.swift returns match
- grep "var cardDescription" Govorun/Models/SuperTextStyle.swift returns match
- grep "extension SuperStyleMode" Govorun/Models/SuperTextStyle.swift returns match
</verification>

<success_criteria>
1. SuperTextStyle.cardDescription returns correct Russian descriptions for all 3 styles
2. SuperStyleMode.displayName returns "Авто" and "Ручной"
3. SettingsSection.textStyle exists with title "Стиль текста", in visibleCases after .general
4. All existing 986+ tests still pass
5. 5 new tests (3 cardDescription + 2 displayName) pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui/08-01-SUMMARY.md`
</output>
