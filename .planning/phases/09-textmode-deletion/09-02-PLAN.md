---
phase: 9
plan: 02
title: "Test cleanup: remove TextMode from all test files"
wave: 2
depends_on: ["09-01"]
files_modified:
  - "GovorunTests/AppContextEngineTests.swift"
  - "GovorunTests/HistoryStoreTests.swift"
  - "GovorunTests/IntegrationTests.swift"
requirements_addressed: [DELETE-01, DELETE-02, DELETE-03, DELETE-04]
autonomous: true
---

# Plan 09-02: Test cleanup: remove TextMode from all test files

<objective>
Remove all TextMode references from test files: delete MockAppModeOverrides, delete tests that assert TextMode mapping, update AppContext construction to 2-field struct, update AppContextEngine init calls. All tests pass with zero TextMode references.
</objective>

<must_haves>
- MockAppModeOverrides class deleted from AppContextEngineTests.swift
- All tests asserting context.textMode deleted from AppContextEngineTests.swift
- AppContextEngine init in tests uses single-param `init(workspace:)` form
- AppContext construction in HistoryStoreTests uses 2-field form (bundleId, appName)
- IntegrationTests uses AppContextEngine(workspace:) without modeOverrides
- `xcodebuild test` passes with zero TextMode references in GovorunTests/
- `grep -rn "TextMode" --include="*.swift" GovorunTests/` returns zero results
</must_haves>

## Tasks

<task id="09-02-01">
<title>Rewrite AppContextEngineTests.swift: remove TextMode tests and mocks</title>
<read_first>
- GovorunTests/AppContextEngineTests.swift
- Govorun/Core/AppContextEngine.swift
</read_first>
<action>
Replace the entire content of `GovorunTests/AppContextEngineTests.swift` with the following. This preserves:
- MockWorkspaceProvider (needed for testing workspace detection)
- makeEngine helper (simplified -- no overrides param)
- Tests 1, 4, 5, 11 rewritten to assert bundleId/appName only (no textMode)
- Tests 7-10 + prompt tests (already use SuperTextStyle, kept as-is)

Deleted entirely:
- MockAppModeOverrides class (lines 17-31)
- Tests 2, 3, 6, 12-18 (only tested TextMode mapping/overrides)

New file content:

```swift
@testable import Govorun
import XCTest

// MARK: - Мок WorkspaceProviding

final class MockWorkspaceProvider: WorkspaceProviding {
    var bundleId: String?
    var appName: String?

    func frontmostApp() -> (bundleId: String?, appName: String?) {
        (bundleId, appName)
    }
}

// MARK: - Тесты AppContextEngine

final class AppContextEngineTests: XCTestCase {
    private func makeEngine(
        bundleId: String? = nil,
        appName: String? = nil
    ) -> (AppContextEngine, MockWorkspaceProvider) {
        let workspace = MockWorkspaceProvider()
        workspace.bundleId = bundleId
        workspace.appName = appName

        let engine = AppContextEngine(workspace: workspace)
        return (engine, workspace)
    }

    // MARK: - 1. Telegram: bundleId и appName определяются

    func test_telegram_detected() {
        let (engine, _) = makeEngine(
            bundleId: "ru.keepcoder.Telegram",
            appName: "Telegram"
        )
        let context = engine.detectCurrentApp()

        XCTAssertEqual(context.bundleId, "ru.keepcoder.Telegram")
        XCTAssertEqual(context.appName, "Telegram")
    }

    // MARK: - 2. Safari: bundleId и appName определяются

    func test_safari_detected() {
        let (engine, _) = makeEngine(
            bundleId: "com.apple.Safari",
            appName: "Safari"
        )
        let context = engine.detectCurrentApp()

        XCTAssertEqual(context.bundleId, "com.apple.Safari")
        XCTAssertEqual(context.appName, "Safari")
    }

    // MARK: - 3. Неизвестное приложение: bundleId и appName определяются

    func test_unknown_app_detected() {
        let (engine, _) = makeEngine(
            bundleId: "com.some.unknown.app",
            appName: "UnknownApp"
        )
        let context = engine.detectCurrentApp()

        XCTAssertEqual(context.bundleId, "com.some.unknown.app")
        XCTAssertEqual(context.appName, "UnknownApp")
    }

    // MARK: - 4. Нет frontmost app → пустые строки

    func test_nil_bundle_id_returns_empty_strings() {
        let (engine, _) = makeEngine(
            bundleId: nil,
            appName: nil
        )
        let context = engine.detectCurrentApp()

        XCTAssertEqual(context.bundleId, "")
        XCTAssertEqual(context.appName, "")
    }

    // MARK: - 5. Промпт зависит от стиля

    func test_prompt_varies_by_mode() {
        let date = Date(timeIntervalSince1970: 1_710_000_000)

        let relaxedPrompt = SuperTextStyle.relaxed.systemPrompt(currentDate: date)
        let formalPrompt = SuperTextStyle.formal.systemPrompt(currentDate: date)

        XCTAssertNotEqual(relaxedPrompt, formalPrompt)
    }

    // MARK: - 6. .relaxed → разговорный стиль

    func test_relaxed_style_uses_conversational_register() {
        let style = SuperTextStyle.relaxed.styleBlock
        XCTAssertTrue(style.contains("разговорный"))
    }

    // MARK: - 7. .formal → деловой стиль

    func test_formal_style_uses_business_register() {
        let style = SuperTextStyle.formal.styleBlock
        XCTAssertTrue(style.contains("деловой"))
    }

    // MARK: - 8. Промпт содержит текущую дату

    func test_prompt_includes_current_date() throws {
        let calendar = Calendar(identifier: .gregorian)
        let components = DateComponents(year: 2_026, month: 3, day: 10)
        let date = try XCTUnwrap(calendar.date(from: components))

        let prompt = SuperTextStyle.normal.systemPrompt(currentDate: date)

        XCTAssertTrue(prompt.contains("10"))
        XCTAssertTrue(prompt.contains("2026"))
    }

    func test_prompt_preserves_command_frame_examples() {
        let prompt = SuperTextStyle.normal.systemPrompt(currentDate: Date())

        XCTAssertTrue(prompt.contains("СОХРАНИ эту рамку"))
        XCTAssertTrue(prompt.contains("«Запиши, что ...» НЕЛЬЗЯ превращать просто в «...»"))
        XCTAssertTrue(prompt.contains("«Подготовь текст: ...» НЕЛЬЗЯ превращать в обычное сообщение без этой рамки"))
        XCTAssertTrue(prompt.contains("«Запиши, что релиз переносится на 23 марта 2026»"))
    }

    func test_prompt_includes_anti_paraphrase_long_form_examples() {
        let prompt = SuperTextStyle.normal.systemPrompt(currentDate: Date())

        XCTAssertTrue(prompt.contains("НЕ компрессируй длинную диктовку"))
        XCTAssertTrue(prompt.contains("НЕ пересказывай и НЕ упрощай инструкцию"))
        XCTAssertTrue(prompt.contains("«Подготовь текст: demo прошло хорошо, но клиент просит добавить экспорт в PDF, офлайн-режим и синхронизацию со своим Jira Server»"))
        XCTAssertTrue(prompt.contains("«Добавь заметку: если Sparkle-обновление не сработает, нужно проверить appcast, подпись, длину enclosure и CURRENT_PROJECT_VERSION»"))
    }

    func test_prompt_correction_examples_preserve_explicit_time_of_day() {
        let prompt = SuperTextStyle.normal.systemPrompt(currentDate: Date())

        XCTAssertTrue(prompt.contains("«позвони в восемь вечера или нет лучше в девять» → «Позвони в девять вечера»"))
        XCTAssertTrue(prompt.contains("«позвони маме в восемь вечера или нет лучше в девять» → «Позвони маме в девять вечера»"))
    }
}
```

Summary of changes:
- **Deleted**: `MockAppModeOverrides` class (was lines 17-31)
- **Deleted**: 14 tests that asserted `context.textMode` (tests 1-6 original, 11-18 original)
- **Kept**: `MockWorkspaceProvider` class (unchanged)
- **Simplified**: `makeEngine()` helper -- removed `overrides` parameter, returns `(AppContextEngine, MockWorkspaceProvider)` tuple (was 3-tuple)
- **Added**: 4 new tests for bundleId/appName detection (replacing the TextMode-specific ones)
- **Kept**: 7 SuperTextStyle prompt tests (unchanged from original tests 7-10 + 3 prompt tests)
- **Renumbered**: MARK comments to sequential 1-8
</action>
<acceptance_criteria>
- `grep -c "TextMode" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "AppModeOverriding" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "MockAppModeOverrides" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "modeOverrides" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "context.textMode" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "textMode(for:" GovorunTests/AppContextEngineTests.swift` returns 0
- `grep -c "class MockWorkspaceProvider" GovorunTests/AppContextEngineTests.swift` returns 1
- `grep -c "class AppContextEngineTests" GovorunTests/AppContextEngineTests.swift` returns 1
- `grep -c "func test_" GovorunTests/AppContextEngineTests.swift` returns 11 (4 detection + 7 prompt)
</acceptance_criteria>
</task>

<task id="09-02-02">
<title>Update HistoryStoreTests.swift: remove textMode from AppContext construction</title>
<read_first>
- GovorunTests/HistoryStoreTests.swift
- Govorun/Core/AppContextEngine.swift
</read_first>
<action>
Two edits in `GovorunTests/HistoryStoreTests.swift`:

**Edit 1 -- makeAppContext helper (around line 43):**
Change:
```swift
    private func makeAppContext(
        appName: String = "Telegram",
        bundleId: String = "ru.keepcoder.Telegram"
    ) -> AppContext {
        AppContext(bundleId: bundleId, appName: appName, textMode: .chat)
    }
```
To:
```swift
    private func makeAppContext(
        appName: String = "Telegram",
        bundleId: String = "ru.keepcoder.Telegram"
    ) -> AppContext {
        AppContext(bundleId: bundleId, appName: appName)
    }
```

**Edit 2 -- Inline AppContext in test_word_count (around line 179):**
Change:
```swift
        try store.save(result, appContext: AppContext(bundleId: "", appName: "", textMode: .universal))
```
To:
```swift
        try store.save(result, appContext: AppContext(bundleId: "", appName: ""))
```

**Do NOT change line 68**: `XCTAssertEqual(item.textMode, "normal")` -- this asserts the String property on HistoryItem, not the TextMode enum. It stays.
</action>
<acceptance_criteria>
- `grep -c "TextMode" GovorunTests/HistoryStoreTests.swift` returns 0
- `grep -c "textMode:" GovorunTests/HistoryStoreTests.swift` returns 0
- `grep -c "item.textMode" GovorunTests/HistoryStoreTests.swift` returns 1 (the String assertion on line 68)
- File still contains `AppContext(bundleId: bundleId, appName: appName)`
- File still contains `AppContext(bundleId: "", appName: "")`
</acceptance_criteria>
</task>

<task id="09-02-03">
<title>Update IntegrationTests.swift: remove MockAppModeOverrides from AppContextEngine init</title>
<read_first>
- GovorunTests/IntegrationTests.swift
</read_first>
<action>
Edit `GovorunTests/IntegrationTests.swift` around lines 85-88.

Change:
```swift
    let appContextEngine = AppContextEngine(
        workspace: mockWorkspace,
        modeOverrides: MockAppModeOverrides()
    )
```
To:
```swift
    let appContextEngine = AppContextEngine(workspace: mockWorkspace)
```
</action>
<acceptance_criteria>
- `grep -c "MockAppModeOverrides" GovorunTests/IntegrationTests.swift` returns 0
- `grep -c "modeOverrides" GovorunTests/IntegrationTests.swift` returns 0
- `grep -c "AppContextEngine(workspace: mockWorkspace)" GovorunTests/IntegrationTests.swift` returns 1
</acceptance_criteria>
</task>

<task id="09-02-04">
<title>Run full test suite and verify zero TextMode references</title>
<read_first>
- GovorunTests/AppContextEngineTests.swift
- GovorunTests/HistoryStoreTests.swift
- GovorunTests/IntegrationTests.swift
</read_first>
<action>
Run the full verification:

```bash
# 1. Zero TextMode enum references in test code
grep -rn "TextMode" --include="*.swift" GovorunTests/
# Expected: ZERO results (item.textMode is a String access, not TextMode enum -- but grep for "TextMode" capital-T-capital-M should return 0)

# 2. Zero deleted symbol references
grep -rn "AppModeOverriding" --include="*.swift" GovorunTests/
grep -rn "MockAppModeOverrides" --include="*.swift" GovorunTests/
grep -rn "UserDefaultsAppModeOverrides" --include="*.swift" GovorunTests/
grep -rn "defaultAppModes" --include="*.swift" GovorunTests/
grep -rn "resolveTextMode" --include="*.swift" GovorunTests/
# Expected: all return ZERO

# 3. Regenerate and test
xcodegen generate
xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation
```

If any test fails, inspect the failure. Common causes:
- Missing `textMode:` in AppContext init (search for `AppContext(` with 3 args)
- MockAppModeOverrides still referenced somewhere
- Test count mismatch (expected: same total minus 14 deleted TextMode tests, plus 4 new detection tests = net -10)
</action>
<acceptance_criteria>
- `grep -rn "TextMode" --include="*.swift" GovorunTests/` returns 0 results
- `grep -rn "AppModeOverriding" --include="*.swift" GovorunTests/` returns 0 results
- `grep -rn "MockAppModeOverrides" --include="*.swift" GovorunTests/` returns 0 results
- `xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation 2>&1 | grep "TEST SUCCEEDED\|Test Suite.*passed"` shows tests passing
- `grep -rn "TextMode" --include="*.swift" Govorun/ GovorunTests/ | grep -v "item.textMode" | grep -v "HistoryItem" | grep -v "HistoryStore" | grep -v "HistoryView"` returns 0 results
</acceptance_criteria>
</task>

## Verification

<verification>
After all tasks complete, run the full Phase 9 verification:

```bash
# === File existence checks ===
test ! -f Govorun/Models/TextMode.swift && echo "TextMode.swift DELETED"
test ! -f Govorun/Views/AppModeSettingsView.swift && echo "AppModeSettingsView.swift DELETED"
test ! -f Govorun/App/NSWorkspaceProvider.swift && echo "NSWorkspaceProvider.swift DELETED"

# === Zero-reference checks (production) ===
echo "=== Production TextMode refs (should be 0 excluding allowed residuals) ==="
grep -rn "TextMode" --include="*.swift" Govorun/ | grep -v "HistoryItem.swift" | grep -v "HistoryStore.swift" | grep -v "HistoryView.swift" | wc -l

echo "=== AppModeOverriding refs ==="
grep -rn "AppModeOverriding" --include="*.swift" Govorun/ | wc -l

echo "=== UserDefaultsAppModeOverrides refs ==="
grep -rn "UserDefaultsAppModeOverrides" --include="*.swift" Govorun/ | wc -l

# === Zero-reference checks (tests) ===
echo "=== Test TextMode refs (should be 0) ==="
grep -rn "TextMode" --include="*.swift" GovorunTests/ | wc -l

echo "=== Test AppModeOverriding refs ==="
grep -rn "AppModeOverriding" --include="*.swift" GovorunTests/ | wc -l

# === Allowed residuals (informational) ===
echo "=== Allowed textMode String references ==="
grep -rn "textMode" --include="*.swift" Govorun/ GovorunTests/
# Should show only: HistoryItem.swift, HistoryStore.swift, HistoryView.swift, HistoryStoreTests.swift(item.textMode)

# === Build + Test ===
xcodegen generate
xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation
```
</verification>
