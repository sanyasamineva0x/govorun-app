---
phase: 01-foundation-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Govorun/Models/SuperTextStyle.swift
  - GovorunTests/SuperTextStyleTests.swift
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - STYLE-05
  - TEST-01

must_haves:
  truths:
    - "SuperTextStyle enum has exactly three cases: relaxed, normal, formal"
    - "SuperStyleMode enum has exactly two cases: auto, manual"
    - "Each style returns .normalization from contract property"
    - "applyDeterministic lowercases first letter for relaxed, uppercases for normal/formal"
    - "systemPrompt() combines basePrompt + styleBlock for each style"
    - "relaxed styleBlock includes all 24 brand aliases and 4 tech term aliases inline"
    - "Unit tests cover enum cases, contract, applyDeterministic, styleBlock, systemPrompt"
  artifacts:
    - path: "Govorun/Models/SuperTextStyle.swift"
      provides: "SuperTextStyle enum, SuperStyleMode enum, alias tables, computed properties, systemPrompt"
      contains: "enum SuperTextStyle: String, CaseIterable, Codable"
    - path: "GovorunTests/SuperTextStyleTests.swift"
      provides: "Unit tests for SuperTextStyle"
      contains: "final class SuperTextStyleTests: XCTestCase"
  key_links:
    - from: "Govorun/Models/SuperTextStyle.swift"
      to: "Govorun/Core/NormalizationGate.swift"
      via: "contract property returns LLMOutputContract.normalization"
      pattern: "var contract: LLMOutputContract"
    - from: "GovorunTests/SuperTextStyleTests.swift"
      to: "Govorun/Models/SuperTextStyle.swift"
      via: "@testable import Govorun"
      pattern: "@testable import Govorun"
---

<objective>
Create the SuperTextStyle enum, SuperStyleMode enum, alias tables, all computed properties (contract, displayName, applyDeterministic, styleBlock, systemPrompt), and comprehensive unit tests.

Purpose: This is the foundational type that all downstream phases (Pipeline, Gate, Postflight, Settings, UI) depend on. Nothing else can proceed until these types exist and are tested.
Output: SuperTextStyle.swift with full implementation, SuperTextStyleTests.swift with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Govorun/Models/TextMode.swift
@Govorun/Models/ProductMode.swift
@Govorun/Core/NormalizationGate.swift

<interfaces>
<!-- Existing types the executor needs to reference -->

From Govorun/Core/NormalizationGate.swift (lines 5-8):
```swift
enum LLMOutputContract: Equatable {
    case normalization
    case rewriting
}
```

From Govorun/Models/ProductMode.swift (pattern reference):
```swift
enum ProductMode: String, CaseIterable, Codable {
    case standard
    case superMode = "super"

    var usesLLM: Bool { self == .superMode }
    var title: String { ... }
    var subtitle: String { ... }
}
```

From Govorun/Models/TextMode.swift (prompt template -- lines 17-188):
```swift
extension TextMode {
    static func basePrompt(currentDate: Date, personalDictionary: [String: String] = [:]) -> String { ... }
    var styleBlock: String { ... }
    func systemPrompt(
        currentDate: Date,
        personalDictionary: [String: String] = [:],
        snippetContext: SnippetContext? = nil,
        appName: String? = nil
    ) -> String { ... }
}
```

From Govorun/Models/TextMode.swift (types that still exist in TextMode.swift -- Phase 2 extracts them):
```swift
enum SnippetPlaceholder {
    static let token = "[[[GOVORUN_SNIPPET]]]"
}

struct SnippetContext: Equatable {
    let trigger: String
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SuperTextStyle enum, SuperStyleMode, alias tables, and simple computed properties with tests</name>
  <files>Govorun/Models/SuperTextStyle.swift, GovorunTests/SuperTextStyleTests.swift</files>
  <read_first>
    - Govorun/Models/ProductMode.swift (enum pattern to follow exactly)
    - Govorun/Core/NormalizationGate.swift (LLMOutputContract definition, lines 5-8)
    - Govorun/Models/TextMode.swift (reference for what we are replacing -- do NOT import or reference TextMode)
    - docs/superpowers/specs/2026-03-29-text-styles-v2-design.md (brand/tech alias tables, lines 86-125)
  </read_first>
  <behavior>
    - Test: SuperTextStyle has exactly 3 cases (relaxed, normal, formal) via CaseIterable.allCases.count == 3
    - Test: rawValues are "relaxed", "normal", "formal"
    - Test: SuperTextStyle conforms to Codable (encode/decode roundtrip)
    - Test: SuperStyleMode has exactly 2 cases (auto, manual)
    - Test: SuperStyleMode rawValues are "auto", "manual"
    - Test: contract returns .normalization for all three styles
    - Test: displayName returns "Расслабленный" for relaxed, "Обычный" for normal, "Формальный" for formal
    - Test: applyDeterministic("привет") returns "привет" for relaxed (lowercase preserved)
    - Test: applyDeterministic("привет") returns "Привет" for normal (uppercased)
    - Test: applyDeterministic("привет") returns "Привет" for formal (uppercased)
    - Test: applyDeterministic("Слово") returns "слово" for relaxed (lowercased)
    - Test: applyDeterministic("") returns "" for all styles (empty string guard)
    - Test: brandAliases has exactly 24 entries
    - Test: techTermAliases has exactly 4 entries
    - Test: brandAliases contains ("Slack", "слак"), ("Telegram", "телега"), ("Python", "питон")
    - Test: techTermAliases contains ("PDF", "пдф"), ("API", "апи"), ("URL", "урл"), ("PR", "пр")
  </behavior>
  <action>
    TDD: Write tests first (RED), then implement (GREEN).

    **Step 1 -- RED: Create GovorunTests/SuperTextStyleTests.swift**

    ```swift
    @testable import Govorun
    import XCTest

    final class SuperTextStyleTests: XCTestCase { ... }
    ```

    Write all tests from the behavior block above. Tests will not compile until Step 2.

    **Step 2 -- GREEN: Create Govorun/Models/SuperTextStyle.swift**

    File structure (per D-01, D-03, D-05):

    ```swift
    import Foundation

    // MARK: - Стиль текста

    enum SuperTextStyle: String, CaseIterable, Codable {
        case relaxed
        case normal
        case formal
    }

    // MARK: - Режим выбора стиля

    enum SuperStyleMode: String, CaseIterable {
        case auto
        case manual
    }
    ```

    Extension with computed properties:

    ```swift
    extension SuperTextStyle {
        var contract: LLMOutputContract {
            // v2: все три -> .normalization (per STYLE-04, D-04)
            .normalization
        }

        var displayName: String {
            switch self {
            case .relaxed: "Расслабленный"
            case .normal: "Обычный"
            case .formal: "Формальный"
            }
        }

        func applyDeterministic(_ text: String) -> String {
            // Per STYLE-05: only initial capitalization
            guard !text.isEmpty else { return text }
            switch self {
            case .relaxed:
                return text.prefix(1).lowercased() + text.dropFirst()
            case .normal, .formal:
                return text.prefix(1).uppercased() + text.dropFirst()
            }
        }
    }
    ```

    Extension with alias tables (per D-05):

    ```swift
    extension SuperTextStyle {
        static let brandAliases: [(original: String, relaxed: String)] = [
            ("Slack", "слак"),
            ("Zoom", "зум"),
            ("Telegram", "телега"),
            ("Jira", "жира"),
            ("Notion", "ношен"),
            ("GitHub", "гитхаб"),
            ("YouTube", "ютуб"),
            ("Google", "гугл"),
            ("WhatsApp", "вотсап"),
            ("Discord", "дискорд"),
            ("Figma", "фигма"),
            ("Docker", "докер"),
            ("Chrome", "хром"),
            ("Safari", "сафари"),
            ("Teams", "тимс"),
            ("Trello", "трелло"),
            ("Confluence", "конфлюенс"),
            ("Excel", "эксель"),
            ("Word", "ворд"),
            ("Photoshop", "фотошоп"),
            ("iPhone", "айфон"),
            ("MacBook", "макбук"),
            ("Windows", "винда"),
            ("Linux", "линукс"),
            ("Python", "питон"),
        ]

        static let techTermAliases: [(original: String, relaxed: String)] = [
            ("PDF", "пдф"),
            ("API", "апи"),
            ("URL", "урл"),
            ("PR", "пр"),
        ]
    }
    ```

    Note: brandAliases has 25 entries (24 from spec + Python). The spec lists exactly 25 brands. Verify count against spec table at lines 86-115 of the design doc. If spec has 25, use 25. The requirement D-05 says "24 brands + 4 tech terms" but the spec table has 25 rows. Use the spec table as source of truth -- count rows carefully.

    Run tests -- all must pass (GREEN).

    **Step 3 -- REFACTOR:** Clean up if needed. Ensure `import Foundation` only (no SwiftUI, no AppKit per layer rules).
  </action>
  <verify>
    <automated>xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation -only-testing:GovorunTests/SuperTextStyleTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Govorun/Models/SuperTextStyle.swift contains `enum SuperTextStyle: String, CaseIterable, Codable`
    - Govorun/Models/SuperTextStyle.swift contains `enum SuperStyleMode: String, CaseIterable`
    - Govorun/Models/SuperTextStyle.swift contains `var contract: LLMOutputContract`
    - Govorun/Models/SuperTextStyle.swift contains `func applyDeterministic(_ text: String) -> String`
    - Govorun/Models/SuperTextStyle.swift contains `static let brandAliases: [(original: String, relaxed: String)]`
    - Govorun/Models/SuperTextStyle.swift contains `static let techTermAliases: [(original: String, relaxed: String)]`
    - Govorun/Models/SuperTextStyle.swift contains `var displayName: String`
    - Govorun/Models/SuperTextStyle.swift does NOT contain `import SwiftUI` or `import AppKit`
    - Govorun/Models/SuperTextStyle.swift does NOT contain `TextMode`
    - GovorunTests/SuperTextStyleTests.swift contains `final class SuperTextStyleTests: XCTestCase`
    - GovorunTests/SuperTextStyleTests.swift contains at least 10 test methods (test_*)
    - xcodebuild test -only-testing:GovorunTests/SuperTextStyleTests exits 0
  </acceptance_criteria>
  <done>SuperTextStyle enum with 3 cases, SuperStyleMode with 2 cases, alias tables (brand + tech), contract, displayName, applyDeterministic all implemented. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: systemPrompt generation (basePrompt + styleBlock) with tests</name>
  <files>Govorun/Models/SuperTextStyle.swift, GovorunTests/SuperTextStyleTests.swift</files>
  <read_first>
    - Govorun/Models/SuperTextStyle.swift (just created in Task 1 -- current state)
    - Govorun/Models/TextMode.swift (basePrompt source to port verbatim, lines 17-121; styleBlock lines 125-149; systemPrompt lines 152-188)
    - docs/superpowers/specs/2026-03-29-text-styles-v2-design.md (style definitions lines 8-21, what style controls lines 28-48)
    - GovorunTests/SuperTextStyleTests.swift (append new tests)
  </read_first>
  <behavior>
    - Test: styleBlock for relaxed contains "бренды → кириллица" instruction
    - Test: styleBlock for relaxed contains all 24+ brand aliases inline (at minimum "слак", "зум", "телега")
    - Test: styleBlock for relaxed contains all 4 tech term aliases inline ("пдф", "апи", "урл", "пр")
    - Test: styleBlock for relaxed contains "НЕ ставь заглавную букву" instruction
    - Test: styleBlock for relaxed contains "Без точки в конце"
    - Test: styleBlock for normal contains "бренды → оригинал" instruction
    - Test: styleBlock for normal does NOT contain "слак" or "зум" (no relaxed aliases)
    - Test: styleBlock for formal contains "сленг раскрывать" instruction
    - Test: styleBlock for formal contains "бренды → оригинал" instruction
    - Test: systemPrompt() contains basePrompt text (verify "постпроцессор голосового ввода" substring)
    - Test: systemPrompt() contains styleBlock text (verify style-specific substring)
    - Test: systemPrompt() with appName includes app context block ("КОНТЕКСТ ПРИЛОЖЕНИЯ")
    - Test: systemPrompt() with snippetContext includes snippet block ("ПОДСТАНОВКА")
    - Test: systemPrompt() without appName does NOT contain "КОНТЕКСТ ПРИЛОЖЕНИЯ"
    - Test: basePrompt contains date (pass fixed date, check "1 января 2025" or similar)
    - Test: basePrompt contains "САМОКОРРЕКЦИЯ" section
    - Test: basePrompt contains "ТРАНСЛИТЕРАЦИЯ" section
    - Test: basePrompt contains "ЧИСЛА, ВАЛЮТЫ И ДАТЫ" section
    - Test: basePrompt with personalDictionary includes entries
  </behavior>
  <action>
    TDD: Write tests first (RED), then implement (GREEN).

    **Step 1 -- RED: Append prompt tests to GovorunTests/SuperTextStyleTests.swift**

    Add a new test class or extend existing with test methods from behavior block. Use a fixed date for deterministic tests:
    ```swift
    private let fixedDate: Date = {
        var components = DateComponents()
        components.year = 2025
        components.month = 1
        components.day = 1
        return Calendar(identifier: .gregorian).date(from: components)!
    }()
    ```

    **Step 2 -- GREEN: Add prompt generation to Govorun/Models/SuperTextStyle.swift**

    Port basePrompt from TextMode.basePrompt() VERBATIM (lines 18-121 of TextMode.swift). The normalization contract is style-independent -- copy it exactly. Key sections that MUST be present:
    - "Ты — постпроцессор голосового ввода"
    - "АБСОЛЮТНЫЙ ЗАПРЕТ"
    - "САМОКОРРЕКЦИЯ:" with all markers and examples
    - "ОЧИСТКА:" with fillers list
    - "ТРАНСЛИТЕРАЦИЯ:" with the note that style block below defines brand rules
    - "ЧИСЛА, ВАЛЮТЫ И ДАТЫ:" with "Эти формы нормализует отдельный deterministic слой ДО тебя"
    - "ПРИМЕРЫ:" with all examples from TextMode.basePrompt
    - "Верни ТОЛЬКО обработанный текст"

    Change in ТРАНСЛИТЕРАЦИЯ section (per D-07): replace "Правило транслитерации брендов задаётся стилевым блоком ниже" -- keep this line, it references the styleBlock that follows.

    Change in ПРИМЕРЫ section: the examples show normal-style behavior ("открой жиру" -> "Открой Jira"). These examples demonstrate the normalization CONTRACT (what LLM should/shouldn't do), not style. Keep them as-is. The styleBlock for relaxed will override brand handling.

    **styleBlock implementation (per D-07 through D-10):**

    ```swift
    var styleBlock: String {
        switch self {
        case .relaxed:
            // Per D-08: full brand + tech term table inline
            var block = """
            Стиль: разговорный, краткий. \
            ПЕРЕОПРЕДЕЛЕНИЕ регистра: НЕ ставь заглавную букву в начале предложения. \
            Пиши всё строчными, как в мессенджере. Без точки в конце.
            ПЕРЕОПРЕДЕЛЕНИЕ транслитерации: бренды и техтермины → кириллица строчными.
            Таблица замен (используй ТОЛЬКО эти формы):
            """
            for alias in Self.brandAliases {
                block += "\n\(alias.original) → \(alias.relaxed)"
            }
            for alias in Self.techTermAliases {
                block += "\n\(alias.original) → \(alias.relaxed)"
            }
            block += "\nПример: «скинь в слак» → «скинь в слак», «открой ноушн» → «открой ношен»."
            return block

        case .normal:
            // Per D-09: minimal
            return """
            Стиль: стандартный. Заглавная буква в начале. Без точки в конце. \
            Транслитерация: бренды и техтермины → оригинальное написание (Slack, Zoom, Jira, PDF, API).
            """

        case .formal:
            // Per D-10: original brands + slang expansion
            return """
            Стиль: деловой, вежливый. Заглавная буква в начале. Точка в конце. \
            Транслитерация: бренды и техтермины → оригинальное написание (Slack, Zoom, Jira, PDF, API). \
            Сленг раскрывать в полные формы (норм → нормально, спс → спасибо, ок → хорошо).
            """
        }
    }
    ```

    **systemPrompt implementation (port from TextMode.systemPrompt, lines 152-188):**

    ```swift
    static func basePrompt(currentDate: Date, personalDictionary: [String: String] = [:]) -> String {
        // PORT VERBATIM from TextMode.basePrompt (lines 18-121)
        // ...
    }

    func systemPrompt(
        currentDate: Date,
        personalDictionary: [String: String] = [:],
        snippetContext: SnippetContext? = nil,
        appName: String? = nil
    ) -> String {
        var prompt = Self.basePrompt(currentDate: currentDate, personalDictionary: personalDictionary)
        prompt += "\n\n" + styleBlock

        if let app = appName, !app.isEmpty {
            prompt += """

            КОНТЕКСТ ПРИЛОЖЕНИЯ:
            Пользователь диктует в приложении «\(app)».
            Учитывай это при выборе тональности и оформления.
            """
        }

        if let snippet = snippetContext {
            prompt += """

            ПОДСТАНОВКА:
            Пользователь использовал голосовое сокращение «\(snippet.trigger)».
            На место этого сокращения вставь РОВНО токен \(SnippetPlaceholder.token) — без кавычек, без изменений.
            НЕ вставляй значение сокращения — только токен.
            Построй естественное предложение вокруг токена.
            Токен должен стоять отдельно, окружённым пробелами или пунктуацией.
            Примеры:
            - "скинь на мой имейл" → "Скинь на мой имейл: \(SnippetPlaceholder.token)"
            - "привет вот мой адрес" → "Привет, мой адрес — \(SnippetPlaceholder.token)."
            - "отправь на мой телефон" → "Отправь на мой телефон: \(SnippetPlaceholder.token)."
            """
        }

        return prompt
    }
    ```

    The systemPrompt references SnippetPlaceholder and SnippetContext which still live in TextMode.swift. This is fine -- they are accessible from the same target. Phase 2 will extract them to separate files.

    Run tests -- all must pass (GREEN).

    **Step 3 -- REFACTOR:** Ensure basePrompt is at least ~80 lines (matching TextMode.basePrompt complexity). If shorter, sections are missing. Check for САМОКОРРЕКЦИЯ, ТРАНСЛИТЕРАЦИЯ, ЧИСЛА sections.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation -only-testing:GovorunTests/SuperTextStyleTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Govorun/Models/SuperTextStyle.swift contains `static func basePrompt(currentDate: Date`
    - Govorun/Models/SuperTextStyle.swift contains `var styleBlock: String`
    - Govorun/Models/SuperTextStyle.swift contains `func systemPrompt(`
    - Govorun/Models/SuperTextStyle.swift contains `snippetContext: SnippetContext?`
    - Govorun/Models/SuperTextStyle.swift contains `appName: String?`
    - Govorun/Models/SuperTextStyle.swift basePrompt contains "постпроцессор голосового ввода"
    - Govorun/Models/SuperTextStyle.swift basePrompt contains "САМОКОРРЕКЦИЯ"
    - Govorun/Models/SuperTextStyle.swift basePrompt contains "ТРАНСЛИТЕРАЦИЯ"
    - Govorun/Models/SuperTextStyle.swift basePrompt contains "ЧИСЛА, ВАЛЮТЫ И ДАТЫ"
    - Govorun/Models/SuperTextStyle.swift relaxed styleBlock contains "слак" and "зум" and "пдф"
    - Govorun/Models/SuperTextStyle.swift normal styleBlock contains "оригинальное написание"
    - Govorun/Models/SuperTextStyle.swift formal styleBlock contains "Сленг раскрывать"
    - GovorunTests/SuperTextStyleTests.swift contains test methods for styleBlock and systemPrompt
    - xcodebuild test -only-testing:GovorunTests/SuperTextStyleTests exits 0
  </acceptance_criteria>
  <done>basePrompt ported verbatim from TextMode. styleBlock implemented per D-07 through D-10 (relaxed has full alias table inline, normal minimal, formal with slang expansion). systemPrompt combines basePrompt + styleBlock + optional app context + optional snippet context. All prompt tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation -only-testing:GovorunTests/SuperTextStyleTests` -- all SuperTextStyle tests pass
2. `xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation` -- full suite (986+ tests) still passes (no regressions)
3. Govorun/Models/SuperTextStyle.swift imports only Foundation
4. No references to TextMode in SuperTextStyle.swift
</verification>

<success_criteria>
- SuperTextStyle enum exists with relaxed/normal/formal, conforms to String, CaseIterable, Codable
- SuperStyleMode enum exists with auto/manual
- Brand alias table has entries matching spec (24-25 brands)
- Tech term alias table has exactly 4 entries (PDF, API, URL, PR)
- contract returns .normalization for all three styles
- applyDeterministic handles capitalization correctly per style
- displayName returns Russian localized names
- basePrompt ported from TextMode.basePrompt verbatim (all sections present)
- styleBlock per D-07..D-10 (relaxed inline table, normal minimal, formal slang)
- systemPrompt combines base + style + optional context
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-types/01-01-SUMMARY.md`
</output>
