---
phase: 03-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - GovorunTests/TestHelpers.swift
  - GovorunTests/PipelineEngineTests.swift
  - GovorunTests/AppContextEngineTests.swift
  - GovorunTests/HistoryStoreTests.swift
  - GovorunTests/SnippetEngineTests.swift
  - GovorunTests/NormalizationPipelineTests.swift
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "MockLLMClient conforms to updated LLMClient protocol with superStyle: SuperTextStyle"
    - "All PipelineEngineTests compile and pass with SuperTextStyle (no TextMode in pipeline assertions)"
    - "HistoryStoreTests build PipelineResult with superStyle: instead of textMode:"
    - "NormalizationPipelineTests call postflight with contract: instead of textMode:"
    - "SnippetEngineTests TextModeSnippetPromptTests renamed and use SuperTextStyle"
    - "AppContextEngineTests TextMode tests remain (AppContext.textMode stays until Phase 9)"
    - "Full test suite (986+ tests) passes with 0 failures"
  artifacts:
    - path: "GovorunTests/TestHelpers.swift"
      provides: "MockLLMClient with superStyle param"
      contains: "superStyle: SuperTextStyle"
    - path: "GovorunTests/NormalizationPipelineTests.swift"
      provides: "postflight tests with contract param"
      contains: "contract: .normalization"
    - path: "GovorunTests/SnippetEngineTests.swift"
      provides: "SuperTextStyle snippet prompt tests"
      contains: "SuperTextStyleSnippetPromptTests"
  key_links:
    - from: "GovorunTests/TestHelpers.swift"
      to: "LLMClient protocol"
      via: "MockLLMClient.normalize conforms to updated protocol"
      pattern: "func normalize.*superStyle.*SuperTextStyle"
    - from: "GovorunTests/HistoryStoreTests.swift"
      to: "PipelineResult"
      via: "makePipelineResult helper with superStyle param"
      pattern: "superStyle:"
    - from: "GovorunTests/NormalizationPipelineTests.swift"
      to: "NormalizationPipeline.postflight"
      via: "contract: .normalization in test calls"
      pattern: "contract: \\.normalization"
---

<objective>
Migrate all test files to compile and pass against the new SuperTextStyle-based pipeline signatures: MockLLMClient, PipelineEngineTests, HistoryStoreTests, NormalizationPipelineTests, SnippetEngineTests, and AppContextEngineTests.

Purpose: TEST-06 requirement -- existing test infrastructure updated to exercise the new SuperTextStyle path. Full test suite green confirms the migration is correct.
Output: 6 test files updated, full test suite passes (986+ tests, 0 failures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pipeline-integration/03-CONTEXT.md
@.planning/phases/03-pipeline-integration/03-RESEARCH.md
@.planning/phases/03-pipeline-integration/03-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these are the new signatures tests must conform to -->

LLMClient protocol (updated):
```swift
protocol LLMClient: Sendable {
    func normalize(_ text: String, superStyle: SuperTextStyle, hints: NormalizationHints) async throws -> String
}
```

PipelineResult (updated):
```swift
struct PipelineResult {
    let sessionId: UUID
    let rawTranscript: String
    let normalizedText: String
    let superStyle: SuperTextStyle?   // was: textMode: TextMode
    let normalizationPath: NormalizationPath
    // ... rest unchanged
}
```

NormalizationPipeline.postflight (updated):
```swift
static func postflight(
    deterministicText: String,
    llmOutput: String,
    contract: LLMOutputContract,       // was: textMode: TextMode
    terminalPeriodEnabled: Bool = true,
    ignoredOutputLiterals: Set<String> = []
) -> NormalizationPipelinePostflight
```

SuperTextStyle (from Phase 1):
```swift
enum SuperTextStyle: String, CaseIterable, Codable {
    case relaxed, normal, formal
    func systemPrompt(currentDate: Date, personalDictionary: [String]?, snippetContext: SnippetContext?, appName: String?) -> String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MockLLMClient + HistoryStoreTests + NormalizationPipelineTests migration</name>
  <files>GovorunTests/TestHelpers.swift, GovorunTests/HistoryStoreTests.swift, GovorunTests/NormalizationPipelineTests.swift</files>
  <read_first>
    - GovorunTests/TestHelpers.swift (MockLLMClient class, normalizeCalls tuple, normalize method)
    - GovorunTests/HistoryStoreTests.swift (makePipelineResult helper, makeAppContext helper, all textMode refs)
    - GovorunTests/NormalizationPipelineTests.swift (all postflight calls with textMode: .universal)
  </read_first>
  <action>
**TestHelpers.swift -- MockLLMClient** (per TEST-06):

1. Change `normalizeCalls` tuple type (line ~32):
```swift
private(set) var normalizeCalls: [(text: String, superStyle: SuperTextStyle, hints: NormalizationHints)] = []
```

2. Change `normalize` method signature (line ~35):
```swift
func normalize(_ text: String, superStyle: SuperTextStyle, hints: NormalizationHints) async throws -> String {
```

3. Inside body, update the append (line ~37):
```swift
normalizeCalls.append((text, superStyle, hints))
```

No other changes to TestHelpers.swift -- other mocks (MockSTTClient, etc.) are unrelated.

**HistoryStoreTests.swift** (per TEST-06):

1. `makePipelineResult` helper function: change `textMode: TextMode = .universal` parameter to `superStyle: SuperTextStyle? = .normal`. In the PipelineResult construction inside, change `textMode: textMode` to `superStyle: superStyle`.

2. Line ~68 assertion: change `XCTAssertEqual(item.textMode, "universal")` to `XCTAssertEqual(item.textMode, "normal")` -- because the default changed from `.universal` to `.normal`, and HistoryStore writes `result.superStyle?.rawValue ?? "none"` which is `"normal"`.

3. Line ~172 direct PipelineResult construction: change `textMode: .universal` to `superStyle: .normal`.

4. Line ~179 AppContext construction: `AppContext(bundleId: "", appName: "", textMode: .universal)` -- LEAVE AS-IS. AppContext still has textMode (deleted in Phase 9). This is NOT a pipeline result consumer.

5. `makeAppContext` helper (line ~42): LEAVE AS-IS. It returns `AppContext(bundleId:, appName:, textMode: .chat)`. AppContext.textMode stays until Phase 9.

**NormalizationPipelineTests.swift** (per D-04, postflight signature change):

All 5 postflight calls use `textMode: .universal`. Replace each with `contract: .normalization`:
- `test_postflight_rejected_output_falls_back_to_deterministic_text`: line ~42
- `test_postflight_happy_path_returns_llm_output`: line ~54
- `test_postflight_applies_surface_canon_before_gate`: line ~67
- `test_postflight_applies_surface_canon_for_quotes_and_percents`: line ~79
- `test_postflight_without_terminal_period_strips_trailing_period`: line ~101

Each change: `textMode: .universal` -> `contract: .normalization`

This is semantically identical because `.universal.llmOutputContract` was `.normalization`.
  </action>
  <verify>
    <automated>cd /Users/sanyasamineva/Desktop/govorun-app && grep -n "superStyle: SuperTextStyle" GovorunTests/TestHelpers.swift && grep -c "textMode" GovorunTests/TestHelpers.swift && grep -n "superStyle" GovorunTests/HistoryStoreTests.swift && grep -c "contract: .normalization" GovorunTests/NormalizationPipelineTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - TestHelpers.swift MockLLMClient contains `normalizeCalls: [(text: String, superStyle: SuperTextStyle, hints: NormalizationHints)]`
    - TestHelpers.swift MockLLMClient contains `func normalize(_ text: String, superStyle: SuperTextStyle, hints: NormalizationHints) async throws -> String`
    - TestHelpers.swift: `grep -c "TextMode" GovorunTests/TestHelpers.swift` returns 0
    - HistoryStoreTests.swift makePipelineResult has `superStyle: SuperTextStyle? = .normal` parameter
    - HistoryStoreTests.swift contains `superStyle: superStyle` in PipelineResult init (not textMode)
    - HistoryStoreTests.swift line ~68 asserts `"normal"` not `"universal"`
    - HistoryStoreTests.swift makeAppContext still contains `textMode: .chat` (AppContext stays)
    - NormalizationPipelineTests.swift: `grep -c "contract: .normalization"` returns 5 (all postflight calls)
    - NormalizationPipelineTests.swift: `grep -c "textMode:"` returns 0
  </acceptance_criteria>
  <done>MockLLMClient uses superStyle: SuperTextStyle. HistoryStoreTests build PipelineResult with superStyle:. NormalizationPipelineTests use contract: .normalization. AppContext.textMode references intentionally preserved (Phase 9 scope).</done>
</task>

<task type="auto">
  <name>Task 2: SnippetEngineTests + AppContextEngineTests migration + full suite verification</name>
  <files>GovorunTests/SnippetEngineTests.swift, GovorunTests/AppContextEngineTests.swift, GovorunTests/PipelineEngineTests.swift</files>
  <read_first>
    - GovorunTests/SnippetEngineTests.swift (TextModeSnippetPromptTests class, TextMode.universal.systemPrompt calls)
    - GovorunTests/AppContextEngineTests.swift (all textMode assertions, TextMode.chat/email/universal refs, TextMode.chat.styleBlock)
    - GovorunTests/PipelineEngineTests.swift (verify if any textMode refs exist after Plan 01 changes)
  </read_first>
  <action>
**SnippetEngineTests.swift** (per TEST-06, Pitfall 5):

1. Rename the test class (line ~513): `TextModeSnippetPromptTests` -> `SuperTextStyleSnippetPromptTests`.

2. In `test_systemPrompt_includes_snippet_placeholder_block` (line ~516): Replace `TextMode.universal.systemPrompt(` with `SuperTextStyle.normal.systemPrompt(`. The `.normal` style is the equivalent of `.universal` -- both produce a standard prompt with the snippet placeholder block.

3. In `test_systemPrompt_without_snippet_has_no_substitution_block` (line ~526): Replace `TextMode.universal.systemPrompt(currentDate: Date())` with `SuperTextStyle.normal.systemPrompt(currentDate: Date())`.

These tests verify that systemPrompt correctly includes/excludes the snippet substitution block. The same behavior exists in SuperTextStyle.

**AppContextEngineTests.swift** (per TEST-06):

This file is the most nuanced. It tests AppContextEngine which still uses TextMode/AppContext.textMode (not deleted until Phase 9, DELETE-03). The tests fall into two categories:

A) **AppContext.textMode assertions (LEAVE AS-IS):** Lines ~66, ~78, ~90, ~102, ~114, ~127, ~202, ~216, ~228, ~240, ~252, ~260, ~275, ~280, ~292 -- these test that AppContextEngine correctly maps bundleId to TextMode. This is Phase 9 scope. Do NOT change.

B) **TextMode prompt tests (UPDATE to SuperTextStyle):** Lines ~135-136 (`TextMode.chat.systemPrompt`, `TextMode.email.systemPrompt`), line ~144 (`TextMode.chat.styleBlock`), line ~150 (`TextMode.email.styleBlock`), lines ~162, ~169, ~178, ~187 (`TextMode.universal.systemPrompt`). These tests verify prompt generation that will be handled by SuperTextStyle going forward.

For category B tests:
- `TextMode.chat.systemPrompt(currentDate: date)` -> `SuperTextStyle.relaxed.systemPrompt(currentDate: date)` (chat maps to relaxed)
- `TextMode.email.systemPrompt(currentDate: date)` -> `SuperTextStyle.formal.systemPrompt(currentDate: date)` (email maps to formal)
- `TextMode.chat.styleBlock` -> `SuperTextStyle.relaxed.styleBlock`
- `TextMode.email.styleBlock` -> `SuperTextStyle.formal.styleBlock`
- `TextMode.universal.systemPrompt(currentDate: date)` -> `SuperTextStyle.normal.systemPrompt(currentDate: date)`
- `TextMode.universal.systemPrompt(currentDate: Date())` -> `SuperTextStyle.normal.systemPrompt(currentDate: Date())`

NOTE: The assertion content may differ slightly because SuperTextStyle style blocks have different wording than TextMode style blocks. Read the test assertions carefully:
- If test asserts `style.contains("\"ты\"")` for chat -> SuperTextStyle.relaxed.styleBlock also contains this (from Phase 1 implementation). Verify by checking SuperTextStyle.swift.
- If test asserts `style.contains("\"Вы\"")` for email -> SuperTextStyle.formal.styleBlock also contains this.
- If test asserts prompt content that is identical between TextMode and SuperTextStyle (same basePrompt) -> direct replacement works.

If any assertion content does NOT match SuperTextStyle output (because SuperTextStyle has a different styleBlock text), update the assertion to match what SuperTextStyle actually produces. The source of truth is `Govorun/Models/SuperTextStyle.swift`.

**PipelineEngineTests.swift:**

Check if any textMode references remain. Based on research, PipelineEngineTests had no direct textMode refs after Phase 2 cleanup. But after Plan 01, PipelineResult now uses `superStyle:` -- verify that any PipelineResult construction or assertion in tests is updated. If `makePipelineResult` helper exists here, update it same as HistoryStoreTests.

After all changes, run full test suite:
```bash
xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation 2>&1 | tail -20
```

All 986+ tests must pass with 0 failures.
  </action>
  <verify>
    <automated>cd /Users/sanyasamineva/Desktop/govorun-app && xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SnippetEngineTests.swift contains `class SuperTextStyleSnippetPromptTests` (not TextModeSnippetPromptTests)
    - SnippetEngineTests.swift contains `SuperTextStyle.normal.systemPrompt(` (not TextMode.universal.systemPrompt)
    - SnippetEngineTests.swift: `grep -c "TextMode" GovorunTests/SnippetEngineTests.swift` returns 0
    - AppContextEngineTests.swift prompt tests use `SuperTextStyle.relaxed/formal/normal` instead of `TextMode.chat/email/universal`
    - AppContextEngineTests.swift AppContext.textMode assertions REMAIN (still reference TextMode via context.textMode -- Phase 9 scope)
    - xcodebuild test output contains "TEST SUCCEEDED" or "** TEST SUCCEEDED **"
    - xcodebuild test output shows 0 failures
  </acceptance_criteria>
  <done>All 6 test files migrated. SnippetEngineTests use SuperTextStyle. AppContextEngineTests prompt tests use SuperTextStyle while AppContext.textMode assertions preserved for Phase 9. Full test suite green (986+ tests, 0 failures). TEST-06 requirement satisfied.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `xcodebuild test -scheme Govorun -destination 'platform=macOS' -skipPackagePluginValidation` -- TEST SUCCEEDED, 0 failures
2. `grep -rn "TextMode" GovorunTests/TestHelpers.swift GovorunTests/NormalizationPipelineTests.swift GovorunTests/SnippetEngineTests.swift` -- 0 results
3. `grep -rn "TextMode" GovorunTests/HistoryStoreTests.swift GovorunTests/PipelineEngineTests.swift` -- only AppContext.textMode references (if any)
4. `grep -rn "TextMode" GovorunTests/AppContextEngineTests.swift` -- only context.textMode and AppContextEngine.textMode assertions (Phase 9 scope)
</verification>

<success_criteria>
- MockLLMClient uses superStyle: SuperTextStyle in normalizeCalls and normalize method (TEST-06)
- HistoryStoreTests use superStyle: in PipelineResult construction (TEST-06)
- NormalizationPipelineTests use contract: .normalization instead of textMode: .universal (TEST-06)
- SnippetEngineTests renamed to SuperTextStyleSnippetPromptTests, use SuperTextStyle (TEST-06)
- AppContextEngineTests prompt tests updated to SuperTextStyle while context.textMode preserved (TEST-06)
- Full test suite passes: 986+ tests, 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-integration/03-02-SUMMARY.md`
</output>
